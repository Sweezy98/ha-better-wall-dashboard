# The public registry, whatever the machine is configured for: the lockfile
# records where every package came from, and a private mirror's URL in it
# is both a leak and something CI cannot fetch from.
registry=https://registry.npmjs.org/
